# Claude Code Context for reveal-project

## Current Session State
**Latest Session State File:** `/workspaces/electrosaur/session-states/2026-01-18-16bit-stable.md`

**Quick Status:**
- **Stable Tag:** v0.14.1-stable (commit 62bd8c3)
- **Status:** 16-bit Lab support working, palettes correct
- **Build ID:** 156720f3-6396-499b-a086-9443edaea280
- **Last Updated:** 2026-01-18 18:47 UTC

## Key Files to Read for Context

1. **Session State (COMPREHENSIVE):**
   - `/workspaces/electrosaur/session-states/2026-01-18-16bit-stable.md`
   - Contains complete session history, technical decisions, failed approaches, code locations

2. **Critical User Directive:**
   - `IMPORTANT-SESSION-STATE-LOCATION.md`
   - NEVER use ~/ for session state, ALWAYS use /workspaces/electrosaur/

3. **Critical Code Locations:**
   - `packages/reveal-adobe/src/api/PhotoshopAPI.js` (16-bit normalization ~267-280)
   - `packages/reveal-adobe/src/index.js` (bit depth routing ~1075-1085)
   - `packages/reveal-core/lib/validation/DocumentValidator.js` (8/16-bit validation)

## Quick Resume Instructions

```bash
cd /workspaces/electrosaur/reveal-project
git status
git describe --tags  # Should show v0.14.1-stable
ls packages/reveal-adobe/dist/  # Verify build present
cat /workspaces/electrosaur/session-states/2026-01-18-16bit-stable.md
```

## Critical Context Summary

### What Works
- 16-bit Lab document support with normalization
- 8-bit Lab document support (no regression)
- Canvas preview with swatch interaction
- All 10 preset files loading correctly

### Known Issues (Accepted)
- Sparse colors (<0.5% of pixels) still get palette slots
- Example: (81, -1, 64) at 0.03% of pixels
- All filtering attempts degraded palette quality
- Decision: Accept sparse colors rather than degrade quality

### DO NOT DO
- ❌ Filter colors before/during/after median cut (5 attempts all failed)
- ❌ Use Reveal.PRESETS pattern (doesn't exist, causes crashes)
- ❌ Skip 16-bit Lab normalization (causes wrong palettes)
- ❌ Reuse canvas context without recreating element

### Critical Technical Details
- **16-bit Lab ranges:** L: 0-32768, a/b: 0-32768 (neutral=16384)
- **Must normalize to:** L: 0-255, a/b: 0-255 (neutral=128)
- **Location:** PhotoshopAPI.js:267-280 (NEVER remove this code)
- **Canvas:** Must destroy and recreate DOM element between runs
- **Preview:** Needs 100ms delayed second render for UXP layout

## Project Structure

```
reveal-project/
├── packages/
│   ├── reveal-adobe/       # Photoshop UXP plugin (main focus)
│   ├── reveal-core/         # Pure JS color separation engines
│   └── reveal-batch/        # CLI batch processor
├── IMPORTANT-SESSION-STATE-LOCATION.md
└── .claude-context.md (this file)
```

## Session State History

All session states stored in: `/workspaces/electrosaur/session-states/`

- `2026-01-18-16bit-stable.md` - 16-bit Lab support stable version
