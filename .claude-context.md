# Reveal Project - Current Session Context

**Last Updated:** 2026-02-24 — `v1.0.3`
**Current Focus:** Navigator Plugin — active development

---

## Recent Session Summary (2026-02-15 → 2026-02-24)

### Add Color to Palette ✅ COMPLETE (`b0d1d78`)
"+" button at end of Palette Surgeon grid opens PS color picker, appends user-chosen color to palette, runs full nearest-neighbor re-separation so pixels redistribute with real coverage. Added colors are first-class — editable, deletable, mergeable. Revert on added swatch removes it entirely (palette shrinks). Max 10 colors enforced. Archetype swap resets added colors. Green "+" badge on added swatches. Production path automatic — added colors flow through `_buildOverriddenPalette()` → `exportProductionConfig()` via expanded baseline.

### Neutral Sovereignty + ROBUST_SALIENCY + Archetype Grouping ✅ COMPLETE (`cbf7a48`)
- **Neutral Sovereignty:** Images with >20% neutral pixels get 1 fixed neutral slot. Median cut runs on chromatic pixels only, preventing white/gray backgrounds from consuming the color budget.
- **ROBUST_SALIENCY centroid strategy:** Chroma winsorization (P90), black protection, achromatic exclusion for warm-dominant images where gamut-clipping outliers inflate centroids.
- **LabMedianCut enhancements:** Neutral isolation pre-split, warm a-axis boost (yellow vs orange separation), C* virtual split axis.
- **Archetype grouping:** `"group"` field (graphic/faithful/dramatic) on all 26 archetypes with filter chips in carousel UI.
- **Auto-derived profiles:** ArchetypeMapper derives profiles from centroid values — no more manual `ARCHETYPE_PROFILES` entries.
- **Warm Sovereign tuning:** CIE2000 metric, refinementPasses=10, warmABoost=5.0.

### Eager ΔE Scoring Increase ✅ COMPLETE (`41cfca6`)
`EAGER_SCORE_COUNT` bumped from 3→8 in SessionState.js. Previously only top 3 DNA-ranked archetypes got ΔE-scored during splash; archetypes ranked #4+ (like Chameleon at ΔE 7.9, better than Warm Sovereign's 8.5) were never scored and sank to carousel bottom. Now 8 archetypes are eagerly scored (~2.8s vs ~1s during splash), giving much more accurate ΔE-based carousel ranking.

### Warm Sovereign Archetype ✅ COMPLETE (`3791150`)
New archetype for single-warm-hue-on-neutral images (golden hour, chestnut horses, amber interiors). High temperature_bias weight (4×), VOLUMETRIC centroid strategy, CIE2000 metric.

### Splash Screen Rework + Rebrand ✅ COMPLETE (`c5e03cf`)
Solid yellow REVEAL text (no rainbow), tagline removed, 512x320 @ 16 frames (278 KB, down from 1428 KB). Bigger text (180pt), 500ms final hold, squeegee disappears on last frame, MIN_SPLASH_MS 5400→2000ms. Menu entry rebranded: **Plugins → Electrosaur → Reveal...**

### RGB↔Lab Consolidation ✅ COMPLETE (`e8e3c7d`)
Consolidated duplicated RGB↔Lab conversions into `LabEncoding.js` as single source of truth. `PosterizationEngine` and `ColorSpace.js` now delegate to `LabEncoding.rgbToLab()` / `LabEncoding.labToRgb()`. ColorSpace.js reduced from 375→166 lines. D65 illuminant converters coexist with existing D50+Bradford bulk encoding functions.

### Interactive Radar HUD ✅ COMPLETE (`78db9a5`)
Drag the green polygon vertices on the 7-axis DNA radar chart to directly tune posterization parameters. 7 HTML `<div>` handles overlay the JPEG radar image, positioned at effective polygon vertices. Dragging projects mouse position onto the axis radial, maps to parameter range, and calls `sessionState.updateParameter()`.

- **Axes:** L-weight, C-weight, targetColors, shadowClamp, speckleRescue, minVolume, blackBias
- **Responsiveness:** Mechanical axes (shadowClamp, speckleRescue, minVolume) ~50ms; structural axes ~400ms
- **Sync:** MechanicalKnobs listens to `parameterChanged` event to keep sliders in sync with radar drags
- **Tooltips:** Custom `<span>` overlay on hover (UXP does not support `title` attribute tooltips)
- **White ring affordances** drawn at effective vertex positions in the JPEG

### Blink Comparator ✅ COMPLETE (`7cf9045`)
Click the preview image to toggle between posterized output and original (pre-posterization) proxy. Hold-click >300ms for continuous blink alternation (400ms interval). Release snaps back to posterized.

- **Original buffer:** `ProxyEngine.getOriginalPreviewRGBA()` — Lab16→Lab8→RGB→RGBA, cached, invalidated on new ingest
- **Visual feedback:** Amber "ORIGINAL" label in top-left corner when showing original
- **Edge cases:** Disabled during highlight mode; snaps back on `previewUpdated` (knob change or archetype swap)

**Critical discovery:** UXP non-modal dialogs do NOT receive keyboard events. Photoshop intercepts all keyboard input at the application level. Originally planned as space-key toggle, pivoted to pointer events on `<img>` element. This affects ALL keyboard shortcuts in non-modal UXP dialogs — only Escape works (handled by UXP dialog framework).

### Posterize-on-Demand ✅ COMPLETE (`7bf596b`)
Carousel eagerly posterizes top 3 archetypes only. Remaining archetypes posterize lazily on click. Reduces initial load time significantly for 25 archetypes.

### Bilateral Filter Fix ✅ COMPLETE (`0903b4c`)
Guard for `bilateralPrefilter: 'none'` — was passing 'none' string to filter constructor. Also removed MIN_COVERAGE layer gate that was suppressing valid low-coverage layers.

### Splash Screen Redesign ✅ COMPLETE (`6beb930`)
Anton font, landscape canvas, hidden root element during splash animation. Earlier iterations: squeegee animation (`fc2d125`, `a3f8d18`), realistic screen printing GIF (`3053f1d`), tagline + white text (`7a18cc7`).

### PosterizationEngine Decomposition ✅ COMPLETE (`db7cc2d`)
Decomposed PosterizationEngine from 4693→2012 lines into 5 focused modules:
- `PosterizationEngine.js` — orchestration
- `CentroidStrategies.js` — SALIENCY/VOLUMETRIC centroid selection
- `HueGapAnalyzer.js` — missing color detection
- `PaletteOptimizer.js` — merge/prune/snap
- `MedianCutPartitioner.js` — recursive box splitting

### Core Refactoring & Tech Debt (`09a82b6`, `6334bc8`)
- Deduplicated base64 utilities across packages
- Standardized logging via `@reveal/core` logger
- Removed stale backup files from reveal-core

### Scoring Progress UI ✅ COMPLETE (`ffa03e5`)
Progress bar, preview cycling through archetype results, card highlighting during scoring.

### Carousel ΔE Consistency (`877c9dd`, `31ae85c`)
Single source of truth for ΔE scoring. Uses config distance metric. Live values synced.

### RevelationError Consolidation (`0adb509`, `2e9d341`)
Mean ΔE calculation consolidated into `RevelationError.js` in reveal-core. Duplicates removed from MetricsCalculator (reveal-batch) and AnalysisUI (reveal-adobe).

### Navigator Tech Debt Cleanup (`d2f4911`, `dc94bf6`, `fad6e75`)
- Inlined Pulse 3 scoring to eliminate async sort race condition
- Single-click carousel (was requiring double-click)
- Ctrl+click for color picker (was plain click)
- Green splash screen fix

### Preview = Production Parity (`32adf65`)
Palette pruning now matches between proxy and production. Metric-aware dedup thresholds.

### Reveal Mk II (Chameleon) Integration (`f886701`)
ΔE-sorted carousel. Chameleon scoring system.

### New Archetypes (`9a84334`, `871b8fa`)
Added: `bold_graphic`, `tenebrist_gold`, `golden_naturalist`, `chiaroscuro`. Tightened `warm_dramatic` centroid.

### Metadata Embedding (`df73b91`)
Structured XMP metadata with human-readable IPTC fields written to Photoshop document on Commit.

### DNA-Driven Adaptive Color Count (`d4fdf35`)
Replaces hardcoded targetColors slider. Archetype DNA determines optimal color count.

### Externalized Engine Tuning (`62d0611`)
Engine tuning parameters (lWeight, cWeight, vibrancyBoost, etc.) moved from code into archetype JSON files. Each archetype defines its own tuning.

### Live E_rev Score (`3450441`)
Revelation Error (E_rev) displayed live in Loupe component during editing.

### Loupe Zoom Dropdown (`81b4341`)
1:1 through 1:8 zoom levels with box-filter downsampling.

### Advanced Parameter Panel (`8d114c2`)
Full parameter parity — all engine parameters exposed in collapsible "Advanced" sections grouped by category (Chroma, Palette, Weights, Tone, Substrate, Noise, Engine).

### Native 16-bit Lab Reads (`6c52e46`)
PhotoshopBridge switched to `componentSize: 16` for Lab reads. No more `lab8to16()` upconversion.

### Color Trapping Integration (`db41b89`)
TrapEngine integrated into production render pipeline.

### Earlier (2026-02-14 → 2026-02-15)
- Palette Surgery bug fixes (`f2c4b36`, `3316822`)
- Delete swatch via Alt+click (`d3aacc5`)
- Progressive Carousel (`2c33e4d`)
- MechanicalKnobs extraction to reveal-core (`07a1799`)
- Dialog lifecycle fix (`aaa3aa9`)

---

## DNA v2.0 System Status

### Archetype Count: 26

### Complete Components ✅
1. **Archetype System** — 26 archetypes with DNA v2.0 centroids + externalized engine tuning + group filtering
2. **Matching Engine** — Pure 40/45/15 scoring (no override gates) + auto-derived profiles
3. **Validation System** — DNAValidator with auto-version detection
4. **Batch Processing** — Full pipeline with bilateral prefilter
5. **Plugin (reveal-adobe)** — Bilateral prefilter + busy cursor + diagnostic logging
6. **Navigator Plugin** — Real-time archetype exploration with 800px proxy preview
7. **Production Render** — Full-res separation with bilateral filter + dithering + trapping
8. **Palette Surgery** — Click to edit, Alt+click to delete, "+" to add colors, revert per-swatch
9. **Progressive Carousel** — Instant cards with async palette fill, posterize-on-demand, filter chips
10. **MechanicalKnobs (shared)** — Extracted to reveal-core, used by proxy + production
11. **Blink Comparator** — Click preview to toggle original vs posterized
12. **Interactive Radar HUD** — Drag vertices to tune 7 parameters along axes
13. **Advanced Panel** — Full parameter exposure grouped by category
14. **Loupe** — Magnified pixel inspection with zoom dropdown + live E_rev
15. **Splash Screen** — Anton font landscape animation during ingest
16. **Metadata Embedding** — Separation manifest + IPTC fields on Commit
17. **Neutral Sovereignty** — Auto-isolates neutrals when >20% of image, preserves chromatic budget
18. **ROBUST_SALIENCY** — Chroma-winsorized centroid strategy for warm-dominant images

---

## Top 10 Architecture Files for Review

| # | File | Purpose |
|---|------|---------|
| 1 | `packages/reveal-core/lib/engines/ProxyEngine.js` | 800px proxy engine — baseline/restore, knobs, palette override, mask-based preview, getPaletteForConfig, getOriginalPreviewRGBA |
| 2 | `packages/reveal-navigator/src/state/SessionState.js` | State coordinator — pulse architecture, progressive palette generation, archetype swaps, production export |
| 3 | `packages/reveal-core/lib/engines/PosterizationEngine.js` | Core quantization orchestrator (decomposed: CentroidStrategies, HueGapAnalyzer, PaletteOptimizer, MedianCutPartitioner) |
| 4 | `packages/reveal-core/lib/engines/SeparationEngine.js` | Pixel-to-palette mapping — distance metrics, dithering, mask generation |
| 5 | `packages/reveal-core/lib/engines/MechanicalKnobs.js` | Shared knob algorithms (minVolume, speckleRescue, shadowClamp) |
| 6 | `packages/reveal-navigator/src/components/RadarHUD.js` | DNA radar chart (pixel-rasterized) + interactive drag handles for 7 parameters |
| 7 | `packages/reveal-navigator/src/components/Preview.js` | jpeg-js proxy preview + blink comparator (click to toggle original/posterized) |
| 8 | `packages/reveal-navigator/src/index.js` | Navigator dialog lifecycle — ingest, progressive carousel, Finalize, dialog open/close |
| 9 | `packages/reveal-navigator/src/bridge/ProductionWorker.js` | Full-res render — bilateral filter, locked palette, trapping, single executeAsModal call |
| 10 | `packages/reveal-core/lib/analysis/ArchetypeMapper.js` | DNA→archetype matching — 40/45/15 weighted scoring |

---

## Key Design Decisions

### UXP Keyboard Limitation (2026-02-23)
UXP non-modal dialogs (`dialog.show()`) do NOT receive keyboard events. Photoshop intercepts all keyboard input at the application level before it reaches the plugin. Only Escape works (handled by UXP dialog framework, not by `keydown` listener). All interactive features must use pointer events instead.

### Blink Comparator via Pointer Events (2026-02-23)
Click preview image to toggle original/posterized. Hold >300ms enters blink mode (alternates every 400ms). Disabled during highlight (swatch isolation). Original RGBA cached in `ProxyEngine._originalRGBA`, invalidated on new ingest.

### Interactive Radar Handles as HTML Overlays (2026-02-23)
Drag handles are `<div>` elements overlaid on the JPEG radar image, not rendered into the JPEG. This gives instant pointer response without waiting for JPEG re-encode. The debounce in `sessionState.updateParameter()` (50ms) throttles engine work.

### Posterize-on-Demand (2026-02-22)
Top 8 archetype cards get eagerly ΔE-scored and posterized (bumped from 3→8 on 2026-02-23). The rest posterize lazily when clicked. With 26 archetypes, eager posterization of all would add several seconds to initial load.

### PosterizationEngine Decomposition (2026-02-21)
Split monolithic 4693-line engine into 5 focused modules. Orchestrator delegates to CentroidStrategies, HueGapAnalyzer, PaletteOptimizer, MedianCutPartitioner.

### Externalized Engine Tuning (2026-02-17)
All tunable parameters (lWeight, cWeight, vibrancyBoost, chromaGate, etc.) moved from hardcoded values into archetype JSON files. Each archetype defines its own tuning profile.

### DNA-Driven Adaptive Color Count (2026-02-17)
`targetColors` no longer hardcoded. Archetype DNA determines optimal color count based on image complexity.

### Palette Override Pipeline (2026-02-14)
Order in `updateProxy`: Restore baseline → Apply palette override → Apply knobs. Overrides indexed against baseline. `_buildOverriddenPalette` reads from `_baselineState.palette`.

### MinVolume Preserves Index Stability (2026-02-14)
Weak colors remapped in colorIndices, palette array length unchanged. Override indices stay valid.

### Production Render Uses Locked Palette (No Re-Posterize)
Palette curated via Surgery Mode. Re-posterizing would invalidate overrides.

### Proxy Preview Uses Mask-Based Rendering
Preview from masks (not raw colorIndices) so speckleRescue effects are visible.

### Color Picker Cancel Detection (2026-02-15)
UXP `showColorPicker` batchPlay doesn't reliably return `_obj:"cancel"` on dismiss. Instead, compare foreground color before/after — Photoshop reverts it on cancel.

### Swatch Deletion (2026-02-15)
Alt+click merges deleted swatch's pixels into nearest CIE76 neighbor. Swatch shows dimmed + red X badge. Revert restores it. Deletion stored in paletteOverrides as `{deleted: true}`.

### Add Color to Palette (2026-02-24)
"+" button appends user color from PS picker → full re-separation (not just palette swap). Added colors are first-class. Revert removes entirely (palette shrinks). No K-means drift risk — `addColorAndReseparate()` bypasses `PosterizationEngine.posterize()`, runs only nearest-neighbor separation on the expanded palette.

### Neutral Sovereignty (2026-02-24)
When neutrals >20% of image, extract into 1 fixed slot. K-means refinement runs only on chromatic pixels to prevent neutral leak-back into warm centroids. ROBUST_SALIENCY uses P90 chroma winsorization to prevent gamut-clipping outliers from inflating centroids.

---

## Navigator Component Map

| Component | File | Role |
|-----------|------|------|
| **Entry point** | `src/index.js` | Dialog lifecycle, ingest orchestration, _clearUI |
| **SessionState** | `src/state/SessionState.js` | Pulse architecture, proxy management, palette generation, original preview buffer |
| **ArchetypeCarousel** | `src/components/ArchetypeCarousel.js` | Progressive card rendering, archetype selection, posterize-on-demand |
| **PaletteSurgeon** | `src/components/PaletteSurgeon.js` | Swatch edit/delete/add/revert UI |
| **MechanicalKnobs** | `src/components/MechanicalKnobs.js` | Slider UI for all parameters + sync via `parameterChanged` event |
| **Preview** | `src/components/Preview.js` | jpeg-js proxy preview + blink comparator (click toggle, hold blink) |
| **RadarHUD** | `src/components/RadarHUD.js` | DNA radar chart (pixel-rasterized) + interactive drag handles (7 axes) + tooltips |
| **Loupe** | `src/components/Loupe.js` | Magnified pixel inspection, zoom dropdown (1:1–1:8), live E_rev score |
| **PhotoshopBridge** | `src/bridge/PhotoshopBridge.js` | Pixel I/O — native 16-bit Lab reads |
| **ProductionWorker** | `src/bridge/ProductionWorker.js` | Full-res render + trapping inside executeAsModal |

---

## Known Issues

1. **ShadowClamp invisible at proxy** — Binary masks at 512px have no intermediate values. Expected behavior.
2. **UXP keyboard events** — Non-modal dialogs receive zero keyboard events (Photoshop intercepts). All interaction must use pointer events.

---

## Integration Tests

`packages/reveal-core/test/integration/end-to-end-posterization.test.js` — 22 tests covering:
- 8-bit and 16-bit pipeline flows
- Golden palette regression (JethroAsMonroe fixture)
- ProxyEngine consistency (ingest → rePosterize)
- Archetype swap behavior
- Separation quality metrics (coverage, orphans)
- MechanicalKnobs algorithms (minVolume, speckleRescue, shadowClamp)
- Green preservation guards (preservedUnifyThreshold)

Test fixture: `test/fixtures/jethro-1600-lab16.tif` (compact 1600x1095, 6.9MB)

Total test suite: **616 tests across 30 test files** (all passing)

---

**Resume work in:** `/workspaces/electrosaur/reveal-project/packages/reveal-navigator`
