# Reveal Project - Current Session Context

**Last Updated:** 2026-03-02
**Current Focus:** Tech debt cleanup — core and navigator

---

## Active Branch: `tech-debt/navigator-cleanup`

### Commits on this branch

| Commit | Description |
|--------|-------------|
| `4947e46` | **Core API cleanup** — input validation on 4 engine entry points, JSDoc @typedef blocks, `_buildTuningFromConfig()` extraction, `CONFIG_CATEGORIES` in ParameterGenerator, saturationBoost alias removed, export consolidation in index.js |
| `8ce89e7` | **Navigator config consolidation** — `getMechanicalKnobs()` on SessionState, `_buildLayers()` refactored from 9 positional args to knobs object, Loupe knob extraction consolidated, `exportProductionConfig()` and `buildManifest()` simplified |
| `0c581ee` | **Knob defaults to core + call cleanup** — `KNOB_DEFAULTS` and `extractMechanicalKnobs()` in ParameterGenerator, SessionState delegates to core, removed dead `labToRgbD50` fallback in Loupe, replaced manual CIE76 in PaletteSurgeon with `Reveal.LabDistance.cie76()` |

### What Changed (Summary)

**Core (`reveal-core`):**
- Input validation on `posterize()`, `mapPixelsToPaletteAsync()`, `initializeProxy()`, `analyze()`
- `_buildTuningFromConfig()` static helper on PosterizationEngine (flat config → nested tuning)
- `CONFIG_CATEGORIES` on ParameterGenerator — canonical field groupings (STRUCTURAL, MECHANICAL, PRODUCTION, UNIMPLEMENTED)
- `KNOB_DEFAULTS` on ParameterGenerator — default values for non-archetype-driven knobs
- `extractMechanicalKnobs(source)` on ParameterGenerator — canonical knob extraction helper
- JSDoc `@typedef` blocks on PosterizationEngine, SeparationEngine, ProxyEngine
- Consolidated exports in `index.js` using `Object.assign`
- `saturationBoost` legacy alias removed
- 7 new validation tests, 1 updated test (empty palette → throws)

**Navigator (`reveal-navigator`):**
- `getMechanicalKnobs()` on SessionState delegates to `ParameterGenerator.extractMechanicalKnobs()`
- `MECHANICAL_KNOB_DEFAULTS` and `PRODUCTION_KNOB_DEFAULTS` imported from core's `KNOB_DEFAULTS`
- `triggerProxyUpdate()` uses `getMechanicalKnobs()` instead of inline object assembly
- `exportProductionConfig()` simplified: calls `_rebuildConfigFromState()` then spreads `currentConfig`
- `buildManifest()` spreads `currentConfig` instead of cherry-picking 5 fields
- `ProductionWorker._buildLayers()` takes `knobs` object instead of 4 positional args
- `ProductionWorker.renderLoupeTile()` uses `getMechanicalKnobs()` instead of picking from `getState()`
- `Loupe.js` — removed dead-code `labToRgbD50` fallback, calls `Reveal.labToRgbD50()` directly
- `PaletteSurgeon.js` — `_deltaE()` delegates to `Reveal.LabDistance.cie76()` instead of manual reimplementation

---

## Parameter Category Architecture

Parameters are classified into mutually exclusive categories, declared in core (`ParameterGenerator.CONFIG_CATEGORIES`):

| Category | Set | Behavior | Examples |
|----------|-----|----------|----------|
| **MECHANICAL** | 3 params | Fast path — mask re-render only | minVolume, speckleRescue, shadowClamp |
| **PRODUCTION** | 2 params | Production render only | trapSize, meshSize |
| **STRUCTURAL** | 25 params | Slow path — full re-posterize | targetColors, engineType, distanceMetric, ... |
| **UNIMPLEMENTED** | 5 params | UI exists, no engine reads | vibrancyThreshold, detailRescue, medianPass, maskProfile, ditherType |

**Knob defaults** declared in core (`ParameterGenerator.KNOB_DEFAULTS`):
- MECHANICAL: `{ minVolume: 0, speckleRescue: 0, shadowClamp: 0 }`
- PRODUCTION: `{ trapSize: 0, meshSize: 230 }`

Navigator imports these — no local copies.

---

## Key Design Decisions

### Config Single Source of Truth in Core (2026-03-02)
`CONFIG_CATEGORIES`, `KNOB_DEFAULTS`, and `extractMechanicalKnobs()` live in `ParameterGenerator`. Navigator imports and delegates rather than maintaining its own copies. Adding a knob means updating core once.

### Navigator→Core API Discipline (2026-03-02)
Navigator must use core's public API (`Reveal.*`) for all color math. No inline reimplementations of distance metrics or color conversions. Found and fixed: manual CIE76 in PaletteSurgeon, dead `labToRgbD50` fallback in Loupe.

### Engine Unification: One Field, One Switch (2026-03-02)
`distilled` is just another case in posterize()'s switch. No `engineMode` anywhere. ProxyEngine has no special routing.

### Palette Consolidation: Commit Time Only (2026-03-02)
Near-duplicate palette entries consolidated only in `exportProductionConfig()`. Never during preview.

### meshSize is Session-Level Equipment (2026-02-26)
Physical property of printing equipment, not archetype-driven. Default 230 TPI.

---

## Top 10 Architecture Files

| # | File | Purpose |
|---|------|---------|
| 1 | `packages/reveal-core/lib/engines/PosterizationEngine.js` | Core quantization — posterize() switch dispatches all engine types |
| 2 | `packages/reveal-core/lib/engines/ProxyEngine.js` | 800px proxy — calls posterize() unconditionally |
| 3 | `packages/reveal-navigator/src/state/SessionState.js` | State coordinator — parameters, proxy, archetype swaps, config export |
| 4 | `packages/reveal-core/lib/engines/SeparationEngine.js` | Pixel-to-palette mapping, dithering, masks |
| 5 | `packages/reveal-core/lib/engines/MechanicalKnobs.js` | Shared knob algorithms |
| 6 | `packages/reveal-core/lib/analysis/ParameterGenerator.js` | DNA→config expert system, CONFIG_CATEGORIES, KNOB_DEFAULTS |
| 7 | `packages/reveal-core/lib/engines/PaletteOps.js` | Palette operations including consolidateNearDuplicates |
| 8 | `packages/reveal-navigator/src/bridge/ProductionWorker.js` | Full-res render + trapping (knobs object API) |
| 9 | `packages/reveal-core/lib/analysis/ArchetypeMapper.js` | 40/45/15 DNA scoring |
| 10 | `packages/reveal-core/test/unit/parameter-flow-contract.test.js` | Parameter naming contract |

---

## Known Issues

1. **Distilled pseudo-archetype BROKEN** — Shows 10-color palette (should be 12) with no yellow and only one blue in live Photoshop. Test fixture (horse 350×512) produces 12 correctly — bug likely in live pipeline path.
2. **ShadowClamp invisible at proxy** — Binary masks at 512px have no intermediate values. Expected.
3. **UXP keyboard events** — Non-modal dialogs receive zero keyboard events.
4. **5 unimplemented params** — vibrancyThreshold, detailRescue, medianPass, maskProfile, ditherType.

---

## Test Suite

Total: **706 tests across 35 test files** (all passing)

Key tests added during tech debt:
- 7 validation tests in `posterization-engine-main.test.js`
- Empty palette rejection in `separation-engine.test.js`

**Resume work in:** `/workspaces/electrosaur/reveal-project`
