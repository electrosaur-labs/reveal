# Reveal Project - Current Session Context

**Last Updated:** 2026-02-12
**Current Focus:** Navigator Plugin (UXP Panel) — Functional MVP

---

## Recent Session Summary

### Navigator Plugin MVP (2026-02-12) ✅ FUNCTIONAL

**Objective:** Build `@reveal/navigator` UXP panel plugin for real-time archetype navigation with 512px proxy preview.

**Status:** Working end-to-end in Photoshop. All archetypes render correct color images.

**See:** `/workspaces/electrosaur/session-states/navigator-plugin-2026-02-12.md` for full details.

**Key features working:**
- Zero-click auto-ingest of active Lab document
- Document change detection (auto re-ingest on switch)
- 512px proxy posterization with proxy-safe config
- Archetype carousel ranked by DNA match score
- Stats panel: colors, ΔE, timing, match score (S/A/P breakdown), DNA signature
- Click-only archetype swap (no hover auto-switch)

**Critical bugs fixed:**
1. UXP `componentSize: 16` loses chroma → always read 8-bit, upconvert
2. Missing `format: 'lab'` → Lab treated as RGB → added to ProxyEngine
3. CIE94/CIE2000 palette collapse at 512px → proxy-safe config overrides
4. Stale UXP buffer copy → use live buffer + ProxyEngine internal proxyBuffer
5. Carousel stale palette on swap → swatches only update on `previewUpdated`

**Files modified in reveal-core:**
- `packages/reveal-core/lib/engines/ProxyEngine.js` — Added `rePosterize()`, proxy-safe configs

**New package:**
- `packages/reveal-navigator/` — Full UXP panel plugin (see session state for file list)

---

### Archetype Pruning & Pipeline Fix (2026-02-11) ✅ COMPLETE

**See:** `/workspaces/electrosaur/session-states/archetype-pruning-prefilter-fix-2026-02-11.md`

---

## DNA v2.0 System Status

### Archetype Count: 17 (terracotta_ochre deleted 2026-02-11)

### Complete Components ✅
1. **Archetype System** — 17 archetypes with DNA v2.0 centroids
2. **Matching Engine** — Pure 40/45/15 scoring (no override gates)
3. **Validation System** — DNAValidator with auto-version detection
4. **Batch Processing** — Full pipeline with bilateral prefilter
5. **Plugin (reveal-adobe)** — Bilateral prefilter + busy cursor + diagnostic logging
6. **Navigator Plugin** — Real-time archetype exploration with proxy preview

---

## Critical File Locations

### Navigator Plugin (NEW)
- **Package:** `packages/reveal-navigator/`
- **Entry:** `packages/reveal-navigator/src/index.js`
- **State:** `packages/reveal-navigator/src/state/SessionState.js`
- **Bridge:** `packages/reveal-navigator/src/bridge/PhotoshopBridge.js`
- **Carousel:** `packages/reveal-navigator/src/components/ArchetypeCarousel.js`
- **Preview:** `packages/reveal-navigator/src/components/Preview.js`
- **Build:** `cd packages/reveal-navigator && npm run build`

### DNA v2.0 Core
- **Archetype Definitions:** `packages/reveal-core/archetypes/*.json` (17 files)
- **Archetype Mapper:** `packages/reveal-core/lib/analysis/ArchetypeMapper.js`
- **ProxyEngine:** `packages/reveal-core/lib/engines/ProxyEngine.js`

### Batch Processing
- **Main Processor:** `packages/reveal-batch/src/posterize-psd.js`
- **Test Images:** `packages/reveal-batch/data/TESTIMAGES/`

### Plugin (reveal-adobe)
- **Main Plugin:** `packages/reveal-adobe/src/index.js`
- **Analysis UI:** `packages/reveal-adobe/src/AnalysisUI.js`

---

## Known Issues

1. **multimeter** — ΔE=26.67, Rev=0.0 with thermonuclear_yellow. Needs investigation.
2. **thermonuclear_yellow density breaches** — 4/4 thermonuclear images have density breaches.
3. **Navigator RadarHUD** — Placeholder only, SVG radar not yet implemented.
4. **Navigator palette surgery** — SessionState methods exist but no UI yet.

---

## Immediate Next Steps

1. Navigator: Implement RadarHUD SVG visualization
2. Navigator: Add parameter sliders (mechanical knobs)
3. Navigator: Add palette surgery UI
4. Navigator: Add production export button
5. Investigate multimeter outlier (Rev=0.0)

---

**Resume work in:** `/workspaces/electrosaur/reveal-project/packages/reveal-navigator`
