# Reveal Project - Current Session Context

**Last Updated:** 2026-02-15
**Current Focus:** Navigator Plugin — active development

---

## Recent Session Summary (2026-02-14 → 2026-02-15)

### Palette Surgery Bug ✅ FIXED
The swatch corruption bug (all swatches acquiring override dots / becoming same color) has been resolved across three commits:
- `f2c4b36` fix: accidental merges and revert button
- `3316822` fix: color picker cancel falsely applying override (foreground color comparison instead of unreliable batchPlay cancel detection)
- `d3aacc5` feat: delete swatch via Alt+click (merge into nearest CIE76 neighbor, dimmed + red X badge, revertable)

### Progressive Carousel ✅ COMPLETE (`2c33e4d`)
Restructured pulse architecture so carousel cards appear immediately after DNA scoring (~50ms) instead of waiting for full proxy init (~400ms):
- Pulse 1: score all archetypes, emit `carouselReady` with name+score+hue dots
- Background: posterize each archetype via `ProxyEngine.getPaletteForConfig()` (read-only, no state mutation)
- Cards receive real palette swatches progressively via `archetypePaletteReady` events
- Generation counter for cancellation on archetype swap

### MechanicalKnobs Extraction ✅ COMPLETE (`07a1799`)
- Extracted `MechanicalKnobs` as shared module in reveal-core (`lib/engines/MechanicalKnobs.js`)
- Used by both ProxyEngine and ProductionWorker (single source of truth for minVolume, speckleRescue, shadowClamp)
- Fixed green-missing bug: restored `preservedUnifyThreshold: 0.5` in ProxyEngine proxy-safe overrides (default 12.0 merged minority green centroids at proxy resolution)
- Added 22 end-to-end integration tests with compact 1600x1095 TIFF fixture (JethroAsMonroe)

### Dialog Lifecycle Fix ✅ COMPLETE (`aaa3aa9`)
- Dialog close: dismiss before `sessionState.reset()` to prevent silent error blocking close
- ProductionWorker: entire pipeline (pixel read → separation → masks → layers) inside single `executeAsModal` call for immediate busy cursor

### Earlier (2026-02-14)
- `a089952` feat: # Colors slider, proxy parity, production workflow
- `dbb07bd` fix: display active color count instead of full palette length
- `877f459` feat: convert to command dialog with production render
- `64bd1d7` fix: shadowClamp and speckleRescue visible at 512px proxy

---

## DNA v2.0 System Status

### Archetype Count: 17 (terracotta_ochre deleted 2026-02-11)

### Complete Components ✅
1. **Archetype System** — 17 archetypes with DNA v2.0 centroids
2. **Matching Engine** — Pure 40/45/15 scoring (no override gates)
3. **Validation System** — DNAValidator with auto-version detection
4. **Batch Processing** — Full pipeline with bilateral prefilter
5. **Plugin (reveal-adobe)** — Bilateral prefilter + busy cursor + diagnostic logging
6. **Navigator Plugin** — Real-time archetype exploration with 512px proxy preview
7. **Production Render** — Full-res separation with bilateral filter + dithering
8. **Palette Surgery** — Click to edit, Alt+click to delete, revert per-swatch ✅ WORKING
9. **Progressive Carousel** — Instant cards with async palette fill ✅ NEW
10. **MechanicalKnobs (shared)** — Extracted to reveal-core, used by proxy + production ✅ NEW

---

## Top 10 Architecture Files for Review

| # | File | Purpose |
|---|------|---------|
| 1 | `packages/reveal-core/lib/engines/ProxyEngine.js` | 512px proxy engine — baseline/restore, knobs, palette override, mask-based preview, getPaletteForConfig |
| 2 | `packages/reveal-navigator/src/state/SessionState.js` | State coordinator — pulse architecture, progressive palette generation, archetype swaps, production export |
| 3 | `packages/reveal-core/lib/engines/PosterizationEngine.js` | Core quantization — Lab median cut, SALIENCY centroid, green rescue |
| 4 | `packages/reveal-core/lib/engines/SeparationEngine.js` | Pixel-to-palette mapping — distance metrics, dithering, mask generation |
| 5 | `packages/reveal-core/lib/engines/MechanicalKnobs.js` | **NEW** — Shared knob algorithms (minVolume, speckleRescue, shadowClamp) |
| 6 | `packages/reveal-navigator/src/components/PaletteSurgeon.js` | Swatch editing — click=edit color, Alt+click=delete, revert per-swatch |
| 7 | `packages/reveal-navigator/src/index.js` | Navigator dialog lifecycle — ingest, progressive carousel, Finalize, dialog open/close |
| 8 | `packages/reveal-navigator/src/components/ArchetypeCarousel.js` | Progressive card rendering — hue dots → real swatches, archetypePaletteReady listener |
| 9 | `packages/reveal-navigator/src/bridge/ProductionWorker.js` | Full-res render — bilateral filter, locked palette, single executeAsModal call |
| 10 | `packages/reveal-core/lib/analysis/ArchetypeMapper.js` | DNA→archetype matching — 40/45/15 weighted scoring |

---

## Key Design Decisions

### Palette Override Pipeline (2026-02-14)
Order in `updateProxy`: Restore baseline → Apply palette override → Apply knobs. Overrides indexed against baseline. `_buildOverriddenPalette` reads from `_baselineState.palette`.

### MinVolume Preserves Index Stability (2026-02-14)
Weak colors remapped in colorIndices, palette array length unchanged. Override indices stay valid.

### Production Render Uses Locked Palette (No Re-Posterize)
Palette curated via Surgery Mode. Re-posterizing would invalidate overrides.

### Proxy Preview Uses Mask-Based Rendering
Preview from masks (not raw colorIndices) so speckleRescue effects are visible.

### Color Picker Cancel Detection (2026-02-15)
UXP `showColorPicker` batchPlay doesn't reliably return `_obj:"cancel"` on dismiss. Instead, compare foreground color before/after — Photoshop reverts it on cancel.

### Progressive Carousel Architecture (2026-02-15)
Two-phase pulse: Pulse 1 emits `carouselReady` with scores only (~50ms). Background loop posterizes each archetype via read-only `getPaletteForConfig()` and emits `archetypePaletteReady` per card. Generation counter invalidates stale work on archetype swap.

### Swatch Deletion (2026-02-15)
Alt+click merges deleted swatch's pixels into nearest CIE76 neighbor. Swatch shows dimmed + red X badge. Revert restores it. Deletion stored in paletteOverrides as `{deleted: true}`.

---

## Navigator Component Map

| Component | File | Role |
|-----------|------|------|
| **Entry point** | `src/index.js` | Dialog lifecycle, ingest orchestration, _clearUI |
| **SessionState** | `src/state/SessionState.js` | Pulse architecture, proxy management, palette generation |
| **ArchetypeCarousel** | `src/components/ArchetypeCarousel.js` | Progressive card rendering, archetype selection |
| **PaletteSurgeon** | `src/components/PaletteSurgeon.js` | Swatch edit/delete/revert UI |
| **MechanicalKnobs** | `src/components/MechanicalKnobs.js` | Slider UI for minVolume/speckleRescue/shadowClamp |
| **Preview** | `src/components/Preview.js` | jpeg-js encoded proxy preview image |
| **RadarHUD** | `src/components/RadarHUD.js` | DNA radar chart (pixel-rasterized) |
| **Loupe** | `src/components/Loupe.js` | **NEW** — Magnified pixel inspection |
| **PhotoshopBridge** | `src/bridge/PhotoshopBridge.js` | Pixel I/O (read Lab, write layers) |
| **ProductionWorker** | `src/bridge/ProductionWorker.js` | Full-res render pipeline inside executeAsModal |

---

## Known Issues

1. **ShadowClamp invisible at proxy** — Binary masks at 512px have no intermediate values. Expected behavior.
2. **multimeter** — ΔE=26.67, Rev=0.0 with thermonuclear_yellow
3. **thermonuclear_yellow density breaches** — 4/4 images

---

## Integration Tests (NEW)

`packages/reveal-core/test/integration/end-to-end-posterization.test.js` — 22 tests covering:
- 8-bit and 16-bit pipeline flows
- Golden palette regression (JethroAsMonroe fixture)
- ProxyEngine consistency (ingest → rePosterize)
- Archetype swap behavior
- Separation quality metrics (coverage, orphans)
- MechanicalKnobs algorithms (minVolume, speckleRescue, shadowClamp)
- Green preservation guards (preservedUnifyThreshold)

Test fixture: `test/fixtures/jethro-1600-lab16.tif` (compact 1600x1095, 6.9MB)

---

**Resume work in:** `/workspaces/electrosaur/reveal-project/packages/reveal-navigator`
