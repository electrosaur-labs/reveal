# Reveal Project - Current Session Context

**Last Updated:** 2026-03-05
**Current Focus:** UI reorg complete (v1.0.2), config parity tests added, open source cleanup planned

---

## Active Branch: `main`

### Recent Commits

| Commit | Description |
|--------|-------------|
| `2b0563c` | **Merge** ui/advanced-panel-reorg → main |
| `f79e5a6` | **feat(navigator):** help system, fixed header, radar hover, reread button |
| `c034b51` | **feat(navigator):** help system with tooltip bubbles and proper descriptions |
| `39c91a6` | **style(navigator):** inline dropdowns with labels on same row |
| `316c767` | **feat(navigator):** UI reorganization — advanced panel, radar HUD, help system |

**Tag:** `v1.0.2` — Navigator UI reorg complete

### What Changed (v1.0.2 — UI Reorganization)

**Navigator UI Overhaul:**
- Help system with `?` toggle buttons and tooltip bubbles on all major controls
- Fixed header that stays visible during scroll (archetype name, radar, help toggle)
- Radar HUD with green hover highlight on draggable handles
- "Reread Document" button in fixed header (always enabled, manual re-ingest)
- "Reset to Defaults" grayed out when not dirty (enabled via `sessionState.isCustomized()`)
- Advanced panel reorganized with collapsible sections
- Inline dropdowns with labels on same row
- Palette surgery help text for palette and suggested colors sections

### What Changed (Config Parity Tests — uncommitted)

**New file:** `packages/reveal-navigator/test/config-parity.test.js`
- Test 1: exportProductionConfig() includes every field ProductionWorker reads
- Test 2: Config round-trip preserves production-relevant fields (STRUCTURAL, MECHANICAL, PRODUCTION)
- Test 3: Every ParameterGenerator field is categorized or explicitly whitelisted
- All 8 new tests passing (116 total navigator tests)

---

## Parameter Category Architecture

Parameters are classified into mutually exclusive categories, declared in core (`ParameterGenerator.CONFIG_CATEGORIES`):

| Category | Count | Behavior | Examples |
|----------|-------|----------|----------|
| **MECHANICAL** | 3 | Fast path — mask re-render only | minVolume, speckleRescue, shadowClamp |
| **PRODUCTION** | 3 | Production render only | trapSize, meshSize, ditherType |
| **STRUCTURAL** | 25 | Slow path — full re-posterize | targetColors, engineType, distanceMetric, ... |
| **UNIMPLEMENTED** | 3 | UI exists, no engine reads | detailRescue, medianPass, maskProfile |

**Note:** `detailRescue` and `medianPass` are mislabeled as UNIMPLEMENTED — BilateralFilter reads `detailRescue`, MedianFilter reads `medianPass`. They should be STRUCTURAL. Fix pending.

**Knob defaults** declared in core (`ParameterGenerator.KNOB_DEFAULTS`):
- MECHANICAL: `{ minVolume: 0, speckleRescue: 0, shadowClamp: 0 }`
- PRODUCTION: `{ trapSize: 0, meshSize: 230 }`

Navigator imports these — no local copies.

---

## Key Design Decisions

### Shared Pixel Processing Utility (2026-03-02)
`pixelProcessing.js` in `src/utils/` holds shared constants (`DIM_COLOR`, `BG_COLOR`) and the `applySuggestionGhost()` pixel loop. Both SessionState (proxy preview) and Loupe (native-res tile) delegate to it. No more duplicate 47-line ghost loops.

### Config Single Source of Truth in Core (2026-03-02)
`CONFIG_CATEGORIES`, `KNOB_DEFAULTS`, and `extractMechanicalKnobs()` live in `ParameterGenerator`. Navigator imports and delegates rather than maintaining its own copies.

### Navigator→Core API Discipline (2026-03-02)
Navigator must use core's public API (`Reveal.*`) for all color math. No inline reimplementations of distance metrics or color conversions.

### Engine Unification: One Field, One Switch (2026-03-02)
`distilled` is just another case in posterize()'s switch. No `engineMode` anywhere. ProxyEngine has no special routing.

### Palette Consolidation: Commit Time Only (2026-03-02)
Near-duplicate palette entries consolidated only in `exportProductionConfig()`. Never during preview.

### meshSize is Session-Level Equipment (2026-02-26)
Physical property of printing equipment, not archetype-driven. Default 230 TPI.

---

## Top 10 Architecture Files

| # | File | Purpose |
|---|------|---------|
| 1 | `packages/reveal-core/lib/engines/PosterizationEngine.js` | Core quantization — posterize() switch dispatches all engine types |
| 2 | `packages/reveal-core/lib/engines/ProxyEngine.js` | 800px proxy — calls posterize() unconditionally |
| 3 | `packages/reveal-navigator/src/state/SessionState.js` | State coordinator — 1,805 lines, parameters, proxy, archetype swaps, config export |
| 4 | `packages/reveal-core/lib/engines/SeparationEngine.js` | Pixel-to-palette mapping, dithering, masks |
| 5 | `packages/reveal-core/lib/engines/MechanicalKnobs.js` | Shared knob algorithms |
| 6 | `packages/reveal-core/lib/analysis/ParameterGenerator.js` | DNA→config expert system, CONFIG_CATEGORIES, KNOB_DEFAULTS |
| 7 | `packages/reveal-core/lib/engines/PaletteOps.js` | Palette operations including consolidateNearDuplicates |
| 8 | `packages/reveal-navigator/src/bridge/ProductionWorker.js` | Full-res render + trapping (knobs object API) |
| 9 | `packages/reveal-navigator/src/utils/pixelProcessing.js` | Shared pixel constants (DIM_COLOR, BG_COLOR) and applySuggestionGhost() |
| 10 | `packages/reveal-core/lib/analysis/ArchetypeMapper.js` | 40/45/15 DNA scoring |

---

## Known Issues

1. **Distilled pseudo-archetype BROKEN** — Shows 10-color palette (should be 12) with no yellow and only one blue in live Photoshop. Test fixture (horse 350×512) produces 12 correctly — bug likely in live pipeline path.
2. **ShadowClamp invisible at proxy** — Binary masks at 512px have no intermediate values. Expected.
3. **UXP keyboard events** — Non-modal dialogs receive zero keyboard events.
4. **3 mislabeled params** — `detailRescue` and `medianPass` labeled UNIMPLEMENTED but engines read them; `maskProfile` truly unimplemented.

---

## Next Steps (Planned)

1. **Commit config-parity tests + CONTRIBUTING.md** — Both ready, just need `git add` and commit.

2. **Open source cleanup** — See `/workspaces/electrosaur/opensource-cleanup-plan.md` for full plan. Key items: remove phantom `.d.ts` declaration, normalize author names, add CODE_OF_CONDUCT, create CI workflow, add before/after images to README.

3. **SessionState refactor** — Extract responsibility clusters from 1,805-line god object. Candidates: ArchetypeSwapCoordinator (~150 lines), PaletteEditorFacade (~350 lines), ExportBuilder (~225 lines), MetricsCalculator (~60 lines), ProxyUpdateOrchestrator (~140 lines). Would leave SessionState as ~600-line coordinator.

4. **Fix `detailRescue`/`medianPass` categorization** — Move from UNIMPLEMENTED to STRUCTURAL since BilateralFilter and MedianFilter read them.

---

## Test Suite

Total: **911 tests across 47 core test files + 116 navigator tests** (all passing)

Key test files:
- `SessionState.test.js` — Mock-based unit tests for state coordination
- `ScoringManager.test.js` — Eager set, background scoring
- `parameter-flow-contract.test.js` — Validates CONFIG_CATEGORIES completeness
- `to-engine-options.test.js` — Config→engine options mapping
- `config-parity.test.js` — Navigator↔Core wiring contract (NEW, uncommitted)
- 5 SessionState + real ProxyEngine integration tests (`SessionState.integration.test.js`)
- Distilled golden palette snapshot test (`distilled-horse.test.js`)

**Resume work in:** `/workspaces/electrosaur/reveal-project`
