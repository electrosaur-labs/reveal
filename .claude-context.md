# Reveal Project - Current Session Context

**Last Updated:** 2026-03-03
**Current Focus:** Edge survival metric + Salamander pseudo-archetype (complete)

---

## Active Branch: `main`

### Recent Commits

| Commit | Description |
|--------|-------------|
| `2c5aa48` | **Edge survival metric** — `edgeSurvival16()` structural fidelity metric, wired into live scoring, carousel sorts by quality |
| `7fc290a` | **Salamander DNA-driven colors** — DNA-driven color count (not fixed 12), quality-sorted pinned cards, Salamander in Specialist group |
| `066a6cd` | **Salamander pseudo-archetype** — new pseudo-archetype merging Chameleon DNA + Distilled stability |
| `ce8f193` | **Tech debt** — low-severity fixes, defensive guards, and 144 new tests |
| `9c2a764` | **Bug fixes** — Lab constant dedup, batch bitDepth bug, concurrency queue, carousel cleanup |

### What Changed (Latest Commits)

**Edge Survival Metric (`2c5aa48`):**
- `RevelationError.edgeSurvival16()` — scans H+V adjacent pixel pairs, measures fraction of significant color boundaries (ΔE>15) preserved in posterization
- `ProxyEngine.getPaletteWithQuality()` — returns edge survival alongside ΔE
- `SessionState._computeSortScore()` v2 — `structuralLoss + screenPenalty` where `structuralLoss = (1 - edgeSurvival) × 50`, ΔE fallback before edge data available
- `SessionState.calculateCurrentEdgeSurvival()` — computes edge survival for current separation state
- `ArchetypeCarousel` — cards show edge survival %, score bar driven by quality, pinned cards sort by quality
- 5 new unit tests for `edgeSurvival16`
- Benchmark script: `packages/reveal-batch/horse-edge-survival.js`

**Salamander Pseudo-Archetype (`066a6cd` + `7fc290a`):**
- `generateConfigurationSalamander(dna)` — DNA-interpolated params + VOLUMETRIC centroid + no palette reduction + no preprocessing
- Color count is DNA-driven (inherits from Chameleon's interpolator, typically ~10), NOT fixed like Distilled's 12
- Pinned in carousel position 2 (after Chameleon, Distilled), sorts by quality among pinned group
- In Specialist filter category

---

## Parameter Category Architecture

Parameters are classified into mutually exclusive categories, declared in core (`ParameterGenerator.CONFIG_CATEGORIES`):

| Category | Set | Behavior | Examples |
|----------|-----|----------|----------|
| **MECHANICAL** | 3 params | Fast path — mask re-render only | minVolume, speckleRescue, shadowClamp |
| **PRODUCTION** | 2 params | Production render only | trapSize, meshSize |
| **STRUCTURAL** | 25 params | Slow path — full re-posterize | targetColors, engineType, distanceMetric, ... |
| **UNIMPLEMENTED** | 5 params | UI exists, no engine reads | vibrancyThreshold, detailRescue, medianPass, maskProfile, ditherType |

**Knob defaults** declared in core (`ParameterGenerator.KNOB_DEFAULTS`):
- MECHANICAL: `{ minVolume: 0, speckleRescue: 0, shadowClamp: 0 }`
- PRODUCTION: `{ trapSize: 0, meshSize: 230 }`

Navigator imports these — no local copies.

---

## Key Design Decisions

### Shared Pixel Processing Utility (2026-03-02)
`pixelProcessing.js` in `src/utils/` holds shared constants (`DIM_COLOR`, `BG_COLOR`) and the `applySuggestionGhost()` pixel loop. Both SessionState (proxy preview) and Loupe (native-res tile) delegate to it. No more duplicate 47-line ghost loops.

### Config Single Source of Truth in Core (2026-03-02)
`CONFIG_CATEGORIES`, `KNOB_DEFAULTS`, and `extractMechanicalKnobs()` live in `ParameterGenerator`. Navigator imports and delegates rather than maintaining its own copies.

### Navigator→Core API Discipline (2026-03-02)
Navigator must use core's public API (`Reveal.*`) for all color math. No inline reimplementations of distance metrics or color conversions.

### Engine Unification: One Field, One Switch (2026-03-02)
`distilled` is just another case in posterize()'s switch. No `engineMode` anywhere. ProxyEngine has no special routing.

### Palette Consolidation: Commit Time Only (2026-03-02)
Near-duplicate palette entries consolidated only in `exportProductionConfig()`. Never during preview.

### meshSize is Session-Level Equipment (2026-02-26)
Physical property of printing equipment, not archetype-driven. Default 230 TPI.

---

## Top 10 Architecture Files

| # | File | Purpose |
|---|------|---------|
| 1 | `packages/reveal-core/lib/engines/PosterizationEngine.js` | Core quantization — posterize() switch dispatches all engine types |
| 2 | `packages/reveal-core/lib/engines/ProxyEngine.js` | 800px proxy — calls posterize() unconditionally |
| 3 | `packages/reveal-navigator/src/state/SessionState.js` | State coordinator — parameters, proxy, archetype swaps, config export |
| 4 | `packages/reveal-core/lib/engines/SeparationEngine.js` | Pixel-to-palette mapping, dithering, masks |
| 5 | `packages/reveal-core/lib/engines/MechanicalKnobs.js` | Shared knob algorithms |
| 6 | `packages/reveal-core/lib/analysis/ParameterGenerator.js` | DNA→config expert system, CONFIG_CATEGORIES, KNOB_DEFAULTS |
| 7 | `packages/reveal-core/lib/engines/PaletteOps.js` | Palette operations including consolidateNearDuplicates |
| 8 | `packages/reveal-navigator/src/bridge/ProductionWorker.js` | Full-res render + trapping (knobs object API) |
| 9 | `packages/reveal-navigator/src/utils/pixelProcessing.js` | Shared pixel constants (DIM_COLOR, BG_COLOR) and applySuggestionGhost() |
| 10 | `packages/reveal-core/lib/analysis/ArchetypeMapper.js` | 40/45/15 DNA scoring |

---

## Known Issues

1. **Distilled pseudo-archetype BROKEN** — Shows 10-color palette (should be 12) with no yellow and only one blue in live Photoshop. Test fixture (horse 350×512) produces 12 correctly — bug likely in live pipeline path.
2. **ShadowClamp invisible at proxy** — Binary masks at 512px have no intermediate values. Expected.
3. **UXP keyboard events** — Non-modal dialogs receive zero keyboard events.
4. **5 unimplemented params** — vibrancyThreshold, detailRescue, medianPass, maskProfile, ditherType.

---

## Near-Future Enhancements

1. **Tune composite score formula** — Edge survival wired in, user wants to tune weights later.

---

## Test Suite

Total: **880 tests across 45 core test files + 37 navigator tests across 2 test files** (all passing)

Key tests added during tech debt:
- 7 validation tests in `posterization-engine-main.test.js`
- Empty palette rejection in `separation-engine.test.js`
- 5 SessionState + real ProxyEngine integration tests (`SessionState.integration.test.js`)
- Distilled golden palette snapshot test with ΔE < 2 tolerance (`distilled-horse.test.js`)
- 14 defensive guard tests (`defensive-guards.test.js`)
- 39 SeparationEngine tests (`separation-engine-core.test.js`)
- 28 ArchetypeLoader tests (`archetype-loader.test.js`)
- 13 DensityScanner tests (`density-scanner.test.js`)
- 14 QualityMetrics tests (`quality-metrics.test.js`)
- 12 ImageHeuristicAnalyzer tests (`image-heuristic-analyzer.test.js`)
- 10 PixelAssignment tests (`pixel-assignment.test.js`)
- 14 CropEngine tests (`crop-engine.test.js`)

**Resume work in:** `/workspaces/electrosaur/reveal-project`
