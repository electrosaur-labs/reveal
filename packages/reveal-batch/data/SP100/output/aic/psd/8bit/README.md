# SP100 AIC Posterized PSDs

8-bit Lab separated PSDs generated from AIC inputs by `npm run benchmark:sp100:aic`.

Each image produces a `.psd` (fill+mask layers) and `.json` (DNA, archetype, metrics sidecar).

Do not populate manually — these are generated files.
