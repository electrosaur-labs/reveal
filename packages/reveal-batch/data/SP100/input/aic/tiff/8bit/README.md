# SP100 AIC Source Images

Place 8-bit TIFF files from the Art Institute of Chicago here.

Then run:

```
npm run convert:sp100 aic
```

This converts TIFF → 16-bit Lab PSD in `input/aic/psd/16bit/`.
