{
    "id": "perceptual-optimizer",
    "name": "Perceptual Optimizer",
    "description": "Iterative global Delta-E minimization (K-Means).",
    "steps": [
        { "op": "quantize", "algorithm": "median-cut" },
        { "op": "refine", "algorithm": "k-means", "iterations": {"value": 20, "default": 20} },
        { "op": "map", "dither": "auto" }
    ]
}
